# Decision record: add context system

Date: 2025-12-21

Decision:

- Add a `context/` folder and scripts to generate a **context pack** for local continuation.
- Add a parameter registry + CI gate to enforce grounding.

Rationale:

- Prevents loss of intent and provenance when work is handed over locally.
- Supports publication expectations (transparency, reproducibility).

# Policy interventions encoded in v8

Interventions are applied as parameter shifts (see `docs/policy_levers.csv` and `nhra_game_theory.v8.apply_intervention`).

This intentionally supports a *commissioning-style* question:
> “If we push for **X** in negotiations, what does it do to downstream pressure and strategic incentives (in the model)?”

